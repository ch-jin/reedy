<script type="text/javascript">
  <% if logged_in? %>
    window.currentUser =
      <%= render('api/users/user.json.jbuilder', user: current_user).html_safe %>
  <% end %>

  window.reedyLogoURL = '<%= asset_path 'logo.svg' %>'
  window.guestImg = '<%= asset_path 'guest.svg' %>'
</script>


<div id="root">
  <div class="coming-soon">
    <h1>Reedy</h1>
  </div>
</div>